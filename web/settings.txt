
SAME ROS_DOMAIN_ID 
ROS_LOCALHOST_ONLY=0

essential
ros2 topic list
/tank/state/latest
/tank/api/detect/image_compressed
/tank/api/detect/result

check
ros2 topic echo /tank/state/latest
ros2 topic hz /tank/api/detect/image_compressed


web terminal

source install/setup.bash

export ROS_DOMAIN_ID=0
export ROS_LOCALHOST_ONLY=0

export TANK_WEB_HOST=0.0.0.0
export TANK_WEB_PORT=5001

ros2 run tank_web_dashboard web_dashboard
view=> http://웹컴퓨터IP:5001/view

RVIZ
sudo apt install ros-humble-rosbridge-server
ros2 launch rosbridge_server rosbridge_websocket_launch.xml port:=9090



ROS_COMPUTER
cd ~/TankSimulation
source install/setup.bash

export ROS_DOMAIN_ID=0
export ROS_LOCALHOST_ONLY=0

export TANK_BRIDGE_HOST=0.0.0.0
export TANK_BRIDGE_PORT=5000

# 웹 분리 완료 후에는 기존 우분투 /view 비활성화
export TANK_LIVE_VIEW=false

ros2 run ros_bridge ros_bridge